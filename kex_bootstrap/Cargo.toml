[package]
name = "kex_bootstrap"
version = "0.1.0"
edition = "2021"
# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kex_domain = { path = "../kex_domain" }
kex_infra = { path = "../kex_infra" }
